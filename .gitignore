# ---------- 构建产物 ----------
target/
*.jar
*.war

# ---------- IDE ----------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# ---------- 本地配置（含明文凭证，禁止入库）----------
# application-dev.yml 里有阿里云 OSS 的 AccessKeyId / AccessKeySecret 明文。
# 本仓库是 public，一旦提交就等于公开密钥。
# 新机器上请自行创建该文件，数据库口令已按 ${DB_PASSWORD:...} 走环境变量。
application-dev.yml

# ---------- 日志与临时文件 ----------
*.log
logs/
.DS_Store
Thumbs.db

# ---------- 一次性补丁脚本 ----------
# 这些脚本里硬编码了本机绝对路径，只在本机跑过一次，不进版本库
fix.js
edit_vue.js
write_index.js
edit_frontend.js
fix_dup.js

# ---------- 工具本地配置 ----------
.claude/
